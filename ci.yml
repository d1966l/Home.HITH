- name: Lint Markdown
  continue-on-error: true
  uses: DavidAnson/markdownlint-cli2-action@v19
  with:
    globs: |
      **/*.md
       - name: Lint Markdown
+        continue-on-error: true
         run: markdownlint "**/*.md"
